# Neon Postgres connection string (use the pooled connection from the Neon console).
DATABASE_URL="postgresql://user:password@ep-example-123456-pooler.eu-central-1.aws.neon.tech/dexa?sslmode=require"

# Better Auth. Generate a secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=""
# The site's public URL (http://localhost:5173 in development).
BETTER_AUTH_URL="http://localhost:5173"

# Optional GitHub sign-in. Callback URL: <BETTER_AUTH_URL>/api/auth/callback/github
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
